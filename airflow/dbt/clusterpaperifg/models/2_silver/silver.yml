version: 2

models:
  - name: silver_base_docentes_ifg
    config:
      schema: silver
    description: ta limpo
    columns:
      - name: silver_base_docentes_ifg_disciplina
        data_tests:
          - not_null
      - name: silver_base_docentes_ifg_id
        data_tests:
          - unique
  - name: silver_base_papers
    config:
      schema: silver
    description: muitos papeis