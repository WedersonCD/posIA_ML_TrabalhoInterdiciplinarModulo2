name: clusterpaperifg

profile: clusterpaperifg

model-paths: ["models"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  clusterpaperifg:
    +file_format: delta
    # Materialize staging models as views, and marts as tables
    1_bronze:
      +materialized: view
      +tags: ["bronze"]

    2_silver:
      +schema: silver
      +materialized: table
      +tags: ["silver"]
    
    3_gold:
      +schema: gold
      +materialized: table
      +tags: ["gold"]
    
snapshots:
  clusterpaperifg:
    +file_format: delta
